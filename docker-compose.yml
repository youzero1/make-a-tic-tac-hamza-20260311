version: '3.8'

services:
  tictactoe:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=./data/tictactoe.db
    restart: unless-stopped
